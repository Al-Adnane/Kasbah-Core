version: '3.8'
services:
  api:
    build:
      context: .
      dockerfile: apps/api/Dockerfile
    ports:
      - "8002:8002"
    env_file:
      - .env
    environment:
      - KASBAH_SYSTEM_STABLE=1
      - KASBAH_GEOMETRY_THRESHOLD=70
