Most AI failures in production aren’t hacks — they’re boring mistakes.
An agent has valid access, something shifts mid-run, and the wrong action executes.
Kasbah treats execution as a cryptographic act, not a side effect of auth.
If the proof doesn’t match the intent right now, the action doesn’t run.
